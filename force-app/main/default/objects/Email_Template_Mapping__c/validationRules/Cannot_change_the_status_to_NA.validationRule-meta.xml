<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Cannot_change_the_status_to_NA</fullName>
    <active>false</active>
    <description>Email template mapping status cannot be changed to not activated if the email template is already activated once</description>
    <errorConditionFormula>NOT(ISNULL(Activation_Order__c)) &amp;&amp; TEXT(Status__c) = &apos;Not Activated&apos;</errorConditionFormula>
    <errorDisplayField>Status__c</errorDisplayField>
    <errorMessage>Email Template Mapping cannot be changed to &apos;Not Activated&apos; stage if the template is activated at least once</errorMessage>
</ValidationRule>
